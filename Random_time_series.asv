close all

%Random time series

x = linspace(0, 100, 10000);

y = zeros(length(x),1);
rn = rand();
for i=1:length(x)
    
    if mod(i,100) == 0
        rn = rand();
    end
    
    y(i) = sin(rand()*x(i))^2 + rn*cos(1/4*x(i)) - sin(17/360*x(i))^(3) + rn*3*sin(0.1*x(i)); 

end
plot(x,y)